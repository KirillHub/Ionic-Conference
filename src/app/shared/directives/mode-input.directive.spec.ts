import { ModeInputDirective } from "./mode-input.directive";


describe('CurrencyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ModeInputDirective();
    expect(directive).toBeTruthy();
  });
});
